version: "3.9"

services:
  web:
    build: ./.docker
    container_name: php-quiz
    ports:
      - "8080:80"
    volumes:
      - ./public:/var/www/html
      - ./src:/var/www/src
      - ./db:/var/www/db